{
  "name": "swiggy-backend",
  "version": "1.0.0",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "dev": "NODE_ENV=development nodemon netlify/functions/index.js",
    "start": "node netlify/functions/index.js",
    "build": "npm install && echo 'Backend built successfully'",
    "netlify": "netlify dev",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "express-rate-limit": "^7.5.1",
    "node-fetch": "^2.7.0",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
